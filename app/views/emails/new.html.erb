<h1>New email address for <%= @email.contact.name %></h1>

<% if @email.errors.any? %>
  <h3>Please fix these errors:</h3>
  <ul>
    <% @email.errors.full_messages.each do |message| %>
      <li><%= message %></li>
    <% end %>
  </ul>
<% end %>

<form action="/contacts/<%= @email.contact.id %>/emails" method="post">
  <label for="email_address">Address</lebel>
  <input id="input_color" name="address" type="text" value="<%= @email.address %>">
  <button>Add email</button>
</form>

<p id="phone_two"><a href="/contacts">Return to contact listing</a></p>
<p id="phone_two"><a href="/contacts/<%= @contact.id %>">Return to this contact</a></p>
