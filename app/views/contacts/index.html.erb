<link rel="stylesheet" type="text/css" href="styles_one.css">

<h1>Contacts</h1>

<ul>
  <% @contacts.each do |contact| %>
    <li><a href="/contacts/<%= contact.id %>">
      <%= contact.name %></li>
  <% end %>
</ul>

<a href="/contacts/new">New Contact</a>
